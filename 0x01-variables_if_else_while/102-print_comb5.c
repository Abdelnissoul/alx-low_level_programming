#include <stdio.h>
#include <math.h>
/**
 * main - printing all possible combinations of two two-digit numbers
 * Return: 0 (Success)
 */
int main(void)
{
	int num;
	int num1;
	for (num = 0 ; num <= 98 ; )
}
